# --- CPU-only (local, for benchmark data + non-neural baselines) ---
datasets>=3.0,<4          # datasets 4.x/5.x drift from what trl 0.15 expects
scikit-learn>=1.3
pandas>=2.0
pyarrow>=15.0
numpy>=1.24
reportlab>=4.0            # PDF report generation

# --- GPU (Colab / single consumer GPU, for SFT / DPO / reward model) ---
# Pin TRL: DPOTrainer / RewardTrainer APIs change across releases.
torch>=2.2
# Upper-bound transformers: trl 0.15.x imports MODEL_FOR_VISION_2_SEQ_MAPPING_NAMES,
# which transformers 5.x removed. Unbounded, pip resolves 5.x and TRL fails to import.
transformers>=4.45,<5
trl>=0.12,<0.16
peft>=0.13
accelerate>=1.0
bitsandbytes>=0.43       # 4-bit for the larger gold judge, if used
scipy>=1.11              # Spearman, McNemar
wandb                    # optional experiment logging
matplotlib>=3.8          # result figures (reports/figures/)
