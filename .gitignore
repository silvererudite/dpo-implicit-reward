# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/
*.egg-info/

# Data / model artifacts (regenerated; keep out of git)
data/*.arrow
data/*.parquet
data/cache/
checkpoints/
outputs/
# may be a symlink to fast local storage; "outputs/" alone won't match a symlink
/outputs
wandb/
*.bin
*.safetensors

# HF datasets/transformers cache
.cache/

# Results: keep JSON summaries, ignore large dumps
results/*.parquet
results/raw_scores/

# OS
.DS_Store
